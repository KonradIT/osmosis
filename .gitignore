*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
/reference/

# Signing — NEVER commit the keystore or its passwords
*.jks
*.keystore
/keystore.properties
app-keystore.jks

# Packet captures (may contain WiFi creds) — never commit
*.pcap
*.pcapng
# JVM/msys crash droppings
hs_err_pid*.log
replay_pid*.log
*.stackdump
