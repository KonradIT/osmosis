<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- DayNight so the OS dark theme flips -night resources (Android 10+). On Android 12+ the OsmosisApp
         Application applies Material You dynamic color on top of these, so accent, surfaces and background
         follow the system palette; the values here are the pre-12 (and non-dynamic) fallback. -->
    <style name="Theme.Osmosis" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/osmo_accent</item>
        <item name="colorOnPrimary">@color/osmo_on_accent</item>
        <item name="colorPrimaryContainer">@color/osmo_accent</item>
        <item name="colorOnPrimaryContainer">@color/osmo_on_accent</item>
        <item name="colorSecondary">@color/osmo_accent</item>

        <item name="android:colorBackground">@color/osmo_bg</item>
        <item name="colorSurface">@color/osmo_surface</item>
        <item name="colorSurfaceVariant">@color/osmo_surface_dim</item>
        <item name="colorOnBackground">@color/osmo_ink</item>
        <item name="colorOnSurface">@color/osmo_ink</item>
        <item name="colorOnSurfaceVariant">@color/osmo_muted</item>
        <item name="colorOutline">@color/osmo_track</item>

        <!-- Bars follow the (possibly dynamic) window background; icon polarity flips with the theme.
             statusBarColor/navigationBarColor are honoured on API 29-34 only: from targetSdk 35 the
             window is edge-to-edge and the bars are transparent, so MainActivity pads the root by the
             bar insets instead and the window background shows through. -->
        <item name="android:statusBarColor">?android:attr/colorBackground</item>
        <item name="android:navigationBarColor">?android:attr/colorBackground</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">@bool/osmo_light_system_bars</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">@bool/osmo_light_system_bars</item>
    </style>
</resources>
