<?xml version="1.0" encoding="utf-8"?>
<!-- Per-camera row in the selector. Same pill as [pill_bg] but a 2dp outline so the tappable rows
     read as distinct cards; the status card keeps the thinner [pill_bg]. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="20dp" />
    <stroke android:width="2dp" android:color="?attr/colorOutline" />
</shape>
